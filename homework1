/* 我们在数据库操作的时候，比如 dao 层中当遇到一个 sql.ErrNoRows 的时候，是否应该 Wrap 这个 error，抛给上层。为什么，应该怎么做请写出代码？
   sql.ErrNoRows是查询未得到结果时由Scan返回的错误，在当前代码中应该不方便处理，根据you only handle error once的原则应该抛给上层。
   代码如下：
*/
var ctx context.Context
err := db.QueryRow(ctx, "SELECT ...").Scan(&requirement)
if err != nil {
  fmt.Errorf("%w: No data satisfy %s", err, requirement)
}
